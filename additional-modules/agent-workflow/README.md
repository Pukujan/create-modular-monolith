# Agent Workflow

Development workflow orchestrator for low-context coding agents.

This addon keeps the workflow state machine, dispatch packets, and proof gates separate from the capability registry.

